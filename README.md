# datascience-repo

```json
{
    "principal" : "IAM Principal that will be used for cluster access and provide identity to pods running in experiment namespace.",
    "experiment_size" : "T-shirt size of the experiment - small, medium, large, xlarge",
    "instance_type" : "cpu, arm, nvidia-gpu, trainium"
}
```